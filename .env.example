# Telegram Bot Token from @BotFather (REQUIRED)
# 从 @BotFather 获取的 Telegram 机器人令牌（必需）
BOT_TOKEN=your_telegram_bot_token_here

# Webhook URL (REQUIRED for production deployments)
# This should be the public URL where your bot is hosted
# 生产环境部署的 Webhook URL（生产环境必需）
# 这应该是您机器人托管的公共URL
WEBHOOK_URL=https://your-deployed-url.com

# Development mode (set to "true" for local development with polling)
# 开发模式（设置为 "true" 以使用轮询进行本地开发）
DEV_MODE=true

# Optional: Custom API endpoints (if you want to use a different API than the default)
# 可选：自定义 API 端点（如果您想使用默认以外的API）
# POLLINATIONS_IMAGE_API=https://image.pollinations.ai
# POLLINATIONS_TEXT_API=https://text.pollinations.ai

# Note about WEBHOOK_URL:
# - For Vercel: After first deployment, set this to your Vercel URL
# - For Cloudflare: Set to your Worker URL
# - For local development: Leave this commented out and use DEV_MODE=true
#
# 关于 WEBHOOK_URL 的说明：
# - Vercel部署：首次部署后，设置为您的Vercel URL
# - Cloudflare部署：设置为您的Worker URL
# - 本地开发：保持注释状态并使用 DEV_MODE=true 